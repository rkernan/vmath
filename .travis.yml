sudo: false
language: cpp
os: linux
branches:
  only:
    - master
    - develop
compiler:
  - clang
addons:
  apt:
    sources:
      - llvm-toolchain-precise
      - ubuntu-toolchain-r-test
    packages:
      - clang
      - cmake
      - libboost-dev
      - libboost-test-dev
script:
  - make
  - cd build
  - make 
  - make check
