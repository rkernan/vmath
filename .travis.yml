sudo: required
language: cpp
os: linux

compiler:
    - clang
    - gcc

install:
    - sudo apt-get update
    - sudo apt-get install libboost-dev libboost-test-dev
    - sudo apt-get install doxygen graphviz

script:
    - mkdir build
    - cd build
    - cmake ..
    - make
    - make test
